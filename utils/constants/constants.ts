import { Languages } from '../../interfaces/languagesType'
import { PageMode } from '../../interfaces/pageModeTypes'

export const DEFAULT_LANGUAGE = 'en' as Languages

export const LIGHT_VALUE = 'light'
export const DARK_VALUE = 'dark'
export const DEFAULT_PAGE_MODE = LIGHT_VALUE as PageMode
